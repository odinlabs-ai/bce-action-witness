<!-- bce-agent-context -->
# BCE done-check

Run `npx --no-install bce gate --repo .` (or MCP `run_gate`) before declaring work complete. Fix code on violations. Treat exit 1 and 2 as red. Blueprint, baseline, mode, workflow, waiver, and engine-pin changes are policy changes and require human-owner review.
